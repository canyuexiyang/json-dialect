# v1.1.1 归档与发布说明（2026-09-22）

## 一句话

v1.1.1 的研发产物已按需求全流程归档，README 同步到 v1.1.1，代码与文档推送到 GitHub，
Release v1.1.1 已发布并附上可加载的打包产物。

---

## 一、工作目录文档归档

归档前的问题：`docs/` 里有 v1.1 / v1.1.1 的新文档，但 `docs/README.md` 索引还停在 v1.0.0；
`PRD-v1.3-变更条目.md` 状态仍写「待实施」，而内容实际已并入 PRD 正文 —— **文档双源**。

### 改动清单

| 文件 | 改动 |
|---|---|
| `docs/README.md` | 索引按六个阶段重排，补齐 v1.1 / v1.1.1 全部 7 份文档；依赖关系图补迭代链路；追加「2026-09-22 v1.1.1 归档」说明 |
| `docs/06-迭代/PRD-v1.3-变更条目.md` | 状态「待实施」→「✅ 已合并（存档）」，新增「合并去向」字段，**PRD 双源问题结清** |
| `docs/04-验收/PRD-8-验收标准.md` | 顶部加版本提示：本文件仅覆盖 AC-01~52（v1.0 基线），现行依据为 PRD v1.4 §8（AC-01~80） |
| `docs/03-研发/v1.1-交付总览.md` | 标记为「已被 v1.1.1 取代（版本存档）」；遗留 #5 PRD 合并标记已闭环 |
| `docs/03-研发/v1.1-研发实施记录.md` | 测试数 186 → 187（v1.1 实际口径）；产物状态去掉硬编码 v1.1.0；遗留 #4 标记已闭环 |

### 归档后的目录语义

- `01-需求/` —— 要什么（需求规格）+ 做成什么样算对（**PRD v1.4 为唯一正本**）
- `02-评审/` —— 代码怎么写
- `03-研发/` —— 做（任务清单、spike、实施记录）+ 交付摘要（v1.1 存档 / **v1.1.1 现行**）
- `04-验收/` —— 验（v1.0 基线，现行以 PRD §8 为准）
- `05-缺陷/` —— 真机反馈 → 修复 → 回归
- `06-迭代/` —— 提案 → 决策 → 条目变更（**已合并的条目转存档**）

---

## 二、README.md 更新（v1.0.0 → v1.1.1）

README 之前整整落后一个大版本，下载链接还指向 v1.0.0 的 zip 文件名。

| 区块 | 改动 |
|---|---|
| 快速开始 | 下载链接改为 Releases 页面，不再写死版本号 |
| 门禁表 | 141 测试 / 40 样本 → **197 测试 / 54 样本**，测试文件 8 → 10 |
| 它能做什么 | 新增「三个 tab」表（格式化 / 转义 / 转换）+ 转义 tab ASCII 布局图 + 方向控件说明 |
| 架构 | 架构图补 `toJavaEsc` 分支与「转义 tab 走旁路（text→text，不进 IR）」 |
| 目录结构 | 补 `escapeText.ts` / `logPrefix.ts` / `toJavaEsc.ts`；样本 41 → 54 组 |
| 约束表 | 新增「偏好优先级」（默认值先落同步段，异步偏好覆盖它） |
| 文档索引 | 补 `docs/03-研发/` 与 `docs/06-迭代/` 全部新文档 |
| 当前状态 | 改为**版本演进表**（v1.0.0 / v1.1.0 / v1.1.1 各自要点与门禁）+ 真机冒烟 9 项 |
| 已知遗留 | 4 条人工事项（v1.1.1 验收 / AC-37 / 720×600 视觉 / Spike S-2） |
| 踩过的坑 | 8 条 → **12 条**（新增 Base64 必须走 UTF-8、剥外层引号判据、ESM 不认 NODE_PATH、断言列号要数准） |

已校验：无真实 NUL 控制字符（v1.0.0 时踩过这个坑）。

---

## 三、推送与发布

### 提交

```
bd9204d  feat: v1.1.1 —— 转义 tab 方向可见化 + 外层引号自动剥离
tag      v1.1.1
```

84 个文件变更：src（escapeText / logPrefix / toJavaEsc + 三 tab UI）、tests（+2 文件）、
docs（+7）、samples（+28 日志类）、icons、dist 产物、README。

### 推送

```
env -u HTTPS_PROXY -u https_proxy -u HTTP_PROXY -u http_proxy -u CODEBUDDY_SERVICE_PROXY_URL \
  git -c http.proxy=http://127.0.0.1:56038 -c http.version=HTTP/1.1 push origin main --tags
```

远端 HEAD `bd9204d` 与本地一致 ✅，tag v1.1.1 已同步。

> 本次新发现：GitHub token 就在 macOS 钥匙串里（`git credential.helper = osxkeychain`），
> **不需要 GIT_ASKPASS / GH_TOKEN**，清掉环境代理后直接 push 即可。

### Release

**https://github.com/canyuexiyang/json-dialect/releases/tag/v1.1.1**

- Release ID `393607518`，非 draft / 非 prerelease
- 附件 `json-dialect-dist-v1.1.1.zip` — **180,338 B**，state `uploaded`
- SHA256 `90825220de554143e2a2952d1f86eb76baee011dd77bbbf26928b4751c1a0c4d`
- 正文含：起因、两项改动（方向可见化 / 外层引号剥离）、FR-I5~I7 + AC-74~80、
  门禁结果、安装步骤、待人工事项

---

## 四、门禁复核

发布前重跑 `npm run verify:all`，全绿：

| 项 | 结果 |
|---|---|
| typecheck | 0 错误 |
| test | **197 passed / 197**（10 个文件） |
| verify:python | 54 个输出文件全部可被目标语言解析 |
| check:privacy | 源码 29 文件零网络调用；产物 6 文件无 CDN / 无 `chrome.tabs.create`；权限仅 `storage` |

---

## 五、下一步（均需人工）

| # | 事项 | 角色 |
|---|---|---|
| 1 | 从 Release 下载 zip 装扩展，跑转义 tab 两个方向 | 测试 |
| 2 | AC-37 真机零网络请求验证（DevTools Network 面板） | 测试 |
| 3 | 720×600 弹窗下 tab 条 + 判定条（现 5 元素）视觉验收 | 设计 |
| 4 | Spike S-2 真机 CSP 确认 | 研发 |
